{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES2020",                                  // Compile to a modern version of JavaScript

    /* Modules */
    "module": "commonjs",                                // Use the standard Node.js module system
    "rootDir": "./src",                                  // Where our source TypeScript files will live

    /* Emitting */
    "outDir": "./dist",                                  // Where the compiled JavaScript will go

    /* Interop Constraints */
    "esModuleInterop": true,                             // Allows better compatibility between module systems
    "forceConsistentCasingInFileNames": true,            // Prevents case-sensitivity issues

    /* Type Checking */
    "strict": true,                                      // Enables all strict type-checking options
    "skipLibCheck": true                                 // Skip type checking of declaration files
  }
}